/* MAPA DE NORMALES
					// r += abs(closestFigure->getNormal(hit).getX()*255);
					// g += abs(closestFigure->getNormal(hit).getY()*255);
					// b += abs(closestFigure->getNormal(hit).getZ()*255);

					
					MAPA DE DISTANCIAS
					r += minT * 100;
					g += minT * 100;
					b += minT * 100 
					*/


Eliminar bucle for max rayos DONE
Photon mapping solo elegir photones de la figura en la que hace hit el rayo DONE
kernel DONE
next-event estimation DONE
Memorias + presentación
Limpiar código
Probar figuras extra DONE
Medir tiempos DONE
Quitar debug de sceneLoader
